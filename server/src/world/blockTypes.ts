// Server-side entry point for block definitions; the source of truth lives in /shared.
export { BlockId, BLOCKS, isValidBlockId, isSolid } from '../../../shared/blocks';
export type { BlockDef } from '../../../shared/blocks';
